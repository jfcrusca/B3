{
  "name": "b3-v10",
  "version": "1.0.0",
  "description": "",
  "main": "00_Core_Orchestrator.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node scripts/clean-logs.js",
    "deploy": "npm run clean && clasp push",
    "deploy:force": "npm run clean && clasp push --force"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@types/google-apps-script": "^2.0.12"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
